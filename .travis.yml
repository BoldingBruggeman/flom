# issues with submodules
# https://travis-ci.com/dashboard

dist: bionic
language: python
os: linux

addons:
   apt:
      packages:
#         - netcdf-bin libnetcdf-dev openmpi-bin libopenmpi-dev gfortran
#KB         - gfortran netcdf-bin libnetcdf-dev libnetcdff-dev

python:
  - '3.6'

# Handle git submodules yourself
#KBgit:
#KB    submodules: false

before_install:
   # Use sed to replace the SSH URL with the public URL, then initialize submodules
#KB  - sed -i 's/git@github.com:/https:\/\/github.com\//' .gitmodules
#KB  - git submodule update --init --recursive

script:
  - pwd
  - ls
#KB  - mkdir build && cd build
#KB  - cmake ../ -DCMAKE_Fortran_COMPILER=gfortran
#KB  - make

branches:
  only:
  - devel
